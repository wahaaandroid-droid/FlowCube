/**
 * Puzzle generation (random Hamiltonian walk + endpoint pairing) will live here.
 * Step 1 focuses on drawing; this module is intentionally minimal for now.
 */
export function placeholderSeed(): number {
  return (Math.random() * 0xffffffff) >>> 0
}
